# Tareas: capa de análisis SQL de A1 sobre la sábana

Este documento convierte el diseño en trabajo ejecutable. El corte sigue la sección 7 del diseño: tres PR encadenados a `main`, cada uno con su propio cierre. Los goldens de `outputs/analysis/` quedan fuera del conteo de líneas de autoría, igual que en A0. Los ocho goldens de A0 en `outputs/` son de solo lectura para este cambio: ninguna tarea los edita, solo verifica su hash.

## Pronóstico de carga de revisión

| Campo | Valor |
|---|---|
| Líneas de autoría estimadas | 630 (PR 1) + 405 (PR 2) + 440 (PR 3) = 1,475 en total; los goldens de `outputs/analysis/` (~145 + ~190 + ~96 filas más `report.md`) quedan fuera de este conteo |
| Presupuesto por PR de este repositorio | 800 líneas (`review_budget_lines`, `openspec/config.yaml`) |
| Riesgo respecto al presupuesto | Medio: el PR 1 llega a ~630, el 79% de las 800 líneas disponibles, y el diseño ya anticipa que el SQL de A1.2 puede empujarlo más cerca |
| PR encadenados recomendados | Sí |
| Estrategia de encadenado | PR apilados hacia main; el PR 1 apunta a `main`, y cada PR siguiente apunta a la rama del PR anterior |
| Estrategia de entrega | auto-chain |
| Decisión pendiente antes de aplicar | No: con auto-chain el orquestador procede con la primera rebanada usando la estrategia ya fijada |

Líneas de control exactas para el guardián automatizado, en el formato literal que exige el skill:

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Medium
```

### Unidades de trabajo sugeridas

| Unidad | Meta | PR probable | Comando de prueba enfocada | Arnés en tiempo real | Límite de reversión |
|---|---|---|---|---|---|
| 1 | Corredor de análisis, subcomando `analyze`, contratos de A1.1/A1.2/A1.5 | PR 1 | `python -m pytest -q -m "not dataset" tests/test_analysis_rules.py tests/test_analysis_dataset_numbers.py tests/test_analysis_idempotency.py` | `python -m worky_engine analyze --data-dir data/raw/sistemas --out-dir outputs/analysis` | Eliminar `worky_engine/analysis/`, `worky_engine/sql/analysis/`, el comando `analyze` de `cli.py`, `worky_engine/quality/analysis_contracts.py` y `outputs/analysis/` |
| 2 | `mart_last_touch`, cohorte de retención y atribución por touch | PR 2 | `python -m pytest -q -m "not dataset" tests/test_analysis_rules.py tests/test_analysis_dataset_numbers.py -k "cohort or attribution or a1_03 or a1_04"` | `python -m worky_engine analyze --data-dir data/raw/sistemas --out-dir outputs/analysis` | Eliminar `a1_00_last_touch.sql`, `a1_03_cohort_retention.sql`, `a1_04_attribution.sql` y revertir sus entradas en `runner.py` y `analysis_contracts.py` |
| 3 | Horas negativas, `analysis_exceptions.csv`, A1.7 y `report.md` | PR 3 | `python -m pytest -q -m "not dataset" tests/test_analysis_report.py` | `python -m worky_engine analyze --data-dir data/raw/sistemas --out-dir outputs/analysis` | Eliminar `a1_06_negative_hours.sql`, `worky_engine/analysis/report.py`, la escritura de `report.md`/`analysis_exceptions.csv` en `cli.py`, y revertir la línea agregada en `README.md` |

## PR 1: corredor de análisis, subcomando `analyze`, A1.1, A1.2 y A1.5

Rama: `feat/a1-pr1-analyze-runner`, base `main`. Líneas de autoría estimadas: ~630. Qué revisa primero el revisor: que `analyze` no lea `outputs/` ni dependa de `build`, y que A1.2 excluya el mes de baja marcando `windows_overlap`. Salvaguarda: si el SQL de A1.2 crece y el PR se acerca a 800 líneas, mover A1.5 (consulta y prueba, ~65 líneas) al PR 3, que tiene más holgura.

- [ ] 1.1 Crear `worky_engine/analysis/__init__.py`, que expone `run_analysis` y `format_report`. ~10 líneas. Decisión D5.
- [ ] 1.2 Crear `worky_engine/analysis/runner.py` con `ANALYSIS_FILES` (orden fijo, `a1_00_last_touch.sql` primero aunque no se use aún) y `ANALYSIS_OUTPUTS`, y `run_analysis`, que registra las tablas y corre los archivos SQL sobre la conexión que abre `analyze`. ~85 líneas. Requirement: comando `analyze` sin build previo. Decisiones D1, D2, D3, D5, D6.
- [ ] 1.3 Crear `worky_engine/sql/analysis/a1_01_active_mrr.sql` (dos columnas de MRR y fila de totales con `GROUPING SETS`). ~45 líneas. Requirement A1.1, escenarios "tabla segmentada con dos totales" y "el total imputado coincide con la sábana". Decisión D6.
- [ ] 1.4 Crear `worky_engine/sql/analysis/a1_02_usage_drop.sql` (ventana de tres meses antes del churn, mes de baja excluido, `drop_status` y `windows_overlap`). ~75 líneas. Requirement A1.2, cuatro escenarios. Decisiones D6, D10.
- [ ] 1.5 Crear `worky_engine/sql/analysis/a1_05_orphan_deals.sql` (deals con `hubspot_id` sin empresa real, con el segundo `LEFT JOIN` contra `quarantine_companies` para no marcar como huérfano a un clon remapeado). ~40 líneas. Requirement A1.5, escenario "35 deals huérfanos". Decisión D6.
- [ ] 1.6 Crear `worky_engine/quality/analysis_contracts.py` con `assert_a1_01_total_row_matches_segments`, `assert_a1_01_crm_within_total`, `assert_a1_01_total_matches_master_dataset`, `assert_a1_02_one_row_per_churned_account`, `assert_a1_02_drop_matches_status` y `assert_a1_05_matches_quarantine_deals`, reusando `ContractViolation` de `worky_engine.quality.contracts`. ~70 líneas. Decisiones D12, D14.
- [ ] 1.7 Modificar `worky_engine/cli.py`: agregar `cmd_analyze` y su parser (`--data-dir`, `--out-dir` por omisión `outputs/analysis`, `--db-path` por omisión `.build/worky_analysis.duckdb`), con importación diferida de `duckdb`/`rapidfuzz`, `load_raw_tables`, `resolve_identity(existing_crosswalk=None, reuse_crosswalk=True)`, `assemble_master_dataset`, `run_contracts` de A0 antes de los propios, `run_analysis` y escritura de los CSV ya disponibles en este PR. ~45 líneas. Requirement: comando `analyze` sin build previo, los tres escenarios. Decisiones D1, D3, D4, D14.
- [ ] 1.8 Crear `tests/test_analysis_rules.py` con un fixture mínimo propio (no reutiliza `tests/fixtures/mini_dataset.py`) y una prueba por regla de A1.1, A1.2 y A1.5: dos columnas de MRR con fila de totales; mes de baja excluido de la ventana; ventana inicial como los primeros tres meses con uso; `windows_overlap` en una cuenta de tres meses; `drop_status` cuando la ventana final queda vacía o la cuenta con churn no tiene uso; deal huérfano detectado y deal de un clon en cuarentena no detectado. Prueba: `python -m pytest -q -m "not dataset" tests/test_analysis_rules.py`. ~155 líneas. Requirement "pruebas de comportamiento por consulta", escenario "prueba de fixture por regla".
- [ ] 1.9 Crear `tests/test_analysis_dataset_numbers.py`, marca `dataset`, con las cifras reales de A1.2 (89 cuentas, 30 con `windows_overlap`) y A1.5 (35 filas, 667,251.00). ~60 líneas. Requirement "pruebas de comportamiento por consulta", escenario "prueba marcada dataset con el número real". Decisión D12.
- [ ] 1.10 Crear `tests/test_analysis_idempotency.py`, marca `dataset`: dos corridas de `analyze` en carpetas temporales distintas producen idénticas byte a byte las salidas disponibles en este PR (`a1_01_active_mrr.csv`, `a1_02_usage_drop.csv`, `a1_05_orphan_deals.csv`) entre sí y contra la copia commiteada, y el hash de los ocho archivos de `outputs/` (A0, read-only) es el mismo antes y después de correr `analyze`. ~55 líneas. Requirement: comando `analyze` sin build previo, escenario "dos corridas idénticas". Decisión D15.
- [ ] 1.11 Cerrar PR 1: correr `python -m pytest -q -m "not dataset"`, correr `python -m pytest -q -m dataset tests/test_analysis_dataset_numbers.py tests/test_analysis_idempotency.py`, correr `python -m worky_engine analyze --data-dir data/raw/sistemas --out-dir outputs/analysis` dos veces y confirmar la byte-identidad de los tres CSV, confirmar con `git status --short outputs` (A0, read-only) que no hay cambios, y generar y commitear `outputs/analysis/a1_01_active_mrr.csv`, `a1_02_usage_drop.csv` y `a1_05_orphan_deals.csv`.

## PR 2: `mart_last_touch`, A1.3 y A1.4

Rama: `feat/a1-pr2-cohort-attribution`, base rama del PR 1. Líneas de autoría estimadas: ~405. Qué revisa primero el revisor: que las celdas censuradas de A1.3 queden vacías sin calcular con dato parcial, y que A1.4 atribuya cada deal a dos canales con el empate por `touch_id` en el extremo que le toca a cada modelo.

- [ ] 2.1 Crear `worky_engine/sql/analysis/a1_00_last_touch.sql` con la vista `mart_last_touch` a grano deal, simétrica a `mart_first_touch` pero con el orden y el empate invertidos. ~40 líneas. Decisión D7.
- [ ] 2.2 Crear `worky_engine/sql/analysis/a1_03_cohort_retention.sql` con cohortes por mes de `signup_date`, formato largo (una fila por cohorte y k), `dataset_end` derivado de `dataset_asof` y `cell_status` en `censored` para celdas sin k meses cumplidos. ~70 líneas. Requirement A1.3, dos escenarios. Decisiones D6, D8, D9, D10.
- [ ] 2.3 Crear `worky_engine/sql/analysis/a1_04_attribution.sql` con atribución a grano deal por primer y último touch, conversión a `closedwon`, tasa por canal en ambos modelos y `channel_rank`. ~80 líneas. Requirement A1.4, dos escenarios. Decisión D6, D7.
- [ ] 2.4 Modificar `worky_engine/analysis/runner.py`: agregar `a1_00_last_touch.sql`, `a1_03_cohort_retention.sql` y `a1_04_attribution.sql` a `ANALYSIS_FILES`/`ANALYSIS_OUTPUTS`, en ese orden porque `a1_04` consume la vista de `a1_00`. ~10 líneas. Decisión D6.
- [ ] 2.5 Modificar `worky_engine/quality/analysis_contracts.py`: agregar `assert_a1_03_pct_within_range`, `assert_a1_03_monotone_non_increasing`, `assert_a1_03_retained_within_cohort_size`, `assert_a1_04_rate_within_unit` y `assert_a1_04_models_cover_same_deals`. ~45 líneas. Decisiones D12, D14.
- [ ] 2.6 Modificar `tests/test_analysis_rules.py`: agregar al fixture dos touches con la misma fecha (empate por `touch_id` en los dos extremos), un deal creado antes del primer touch de su empresa, una cohorte reciente que fuerza celdas censuradas y el caso frontera de una empresa que hace churn exactamente en el mes k. Prueba: `python -m pytest -q -m "not dataset" tests/test_analysis_rules.py`. ~120 líneas. Requirement A1.3 (celda censurada, retención por cohorte y k), A1.4 (dos canales por deal, tasas lado a lado y bandera de cambio de ganador).
- [ ] 2.7 Modificar `tests/test_analysis_dataset_numbers.py`, marca `dataset`: agregar que los deals atribuidos son iguales en los dos modelos (997 menos los 35 huérfanos de A1.5) y que A1.3 nunca sube de un k al siguiente dentro de una misma cohorte sobre el dataset real. ~40 líneas. Decisión D12.
- [ ] 2.8 Modificar `tests/test_analysis_idempotency.py`, marca `dataset`: extender la comparación byte a byte a `a1_03_cohort_retention.csv` y `a1_04_attribution.csv`, y mantener la verificación del hash de los ocho archivos de `outputs/` (A0, read-only). Decisión D15. Nota: el diseño no listó esta modificación en su tabla de líneas del PR 2; se agrega aquí para no dejar sin cobertura las salidas nuevas hasta el cierre del PR 3 (ver brecha en el resumen de esta fase).
- [ ] 2.9 Cerrar PR 2: correr `python -m pytest -q -m "not dataset"`, correr `python -m pytest -q -m dataset tests/test_analysis_dataset_numbers.py tests/test_analysis_idempotency.py`, correr `analyze` dos veces y confirmar byte-identidad de las cinco salidas acumuladas, confirmar `git status --short outputs` (A0, read-only) sin cambios, y generar y commitear `outputs/analysis/a1_03_cohort_retention.csv` y `a1_04_attribution.csv`.

## PR 3: A1.6, `analysis_exceptions.csv`, A1.7 y `report.md`

Rama: `feat/a1-pr3-exceptions-report`, base rama del PR 2. Líneas de autoría estimadas: ~440. Qué revisa primero el revisor: la justificación de tres a cuatro líneas de A1.6, que el bloque de SQL de cada sección de `report.md` sea el texto exacto del archivo `.sql`, y que ningún golden de A0 haya cambiado.

- [ ] 3.1 Crear `worky_engine/sql/analysis/a1_06_negative_hours.sql` con `analysis_a1_06_negative_hours` (tickets con `resolution_hours < 0`, con `resolution_hours_abs`) y la vista `analysis_exceptions` con las once columnas de `exceptions_log`. ~60 líneas. Requirement A1.6, tres escenarios. Decisiones D6, D11.
- [ ] 3.2 Modificar `worky_engine/analysis/runner.py`: agregar `a1_06_negative_hours.sql` a `ANALYSIS_FILES`/`ANALYSIS_OUTPUTS` e incluir `analysis_exceptions` entre las tablas materializadas. ~10 líneas. Decisión D6.
- [ ] 3.3 Crear `worky_engine/analysis/report.py` con `format_report`, sin abrir conexión ni leer archivos: encabezado (motor, `dataset_asof`, `ruleset_version`, comando, conteo de filas), una sección por ítem de A1.1 a A1.6 con definición del ADR-004, motor, el texto del `.sql` que ya leyó el corredor y la tabla de resultado (completa en A1.1/A1.3/A1.4/A1.5, primeras 15 filas más total en A1.2/A1.6), la matriz pivoteada de A1.3 con celdas censuradas vacías, la justificación de A1.6 en 3 a 4 líneas, y la sección A1.7 con el DDL ilustrativo marcado como no ejecutable. ~180 líneas. Requirement "reporte de resultados por ítem" (dos escenarios), A1.7 (un escenario). Decisiones D8, D13.
- [ ] 3.4 Modificar `worky_engine/cli.py`: `cmd_analyze` escribe `analysis_exceptions.csv` y `outputs/analysis/report.md`, y ajusta el mensaje final a `analyze: 6 consultas escritas en <out-dir>`. ~8 líneas. Requirement: comando `analyze` sin build previo.
- [ ] 3.5 Modificar `worky_engine/quality/analysis_contracts.py`: agregar `assert_a1_06_all_hours_negative` y `assert_analysis_exceptions_shape`. ~30 líneas. Decisiones D12, D14.
- [ ] 3.6 Modificar `tests/test_analysis_rules.py`: agregar al fixture un ticket con `resolution_hours` negativo y la prueba de fixture de A1.6 (queda en el detalle, genera su fila de excepción, no entra al promedio de resolución, sí cuenta en conteos y CSAT). ~25 líneas (no incluidas en la tabla de líneas del diseño; ver brecha en el resumen de esta fase). Requirement A1.6, escenarios "48 tickets listados y neutralizados en promedios" y "tickets conservados en conteos y CSAT"; Requirement "pruebas de comportamiento por consulta", escenario "prueba de fixture por regla".
- [ ] 3.7 Crear `tests/test_analysis_report.py`: una sección por ítem en `report.md`, el bloque de SQL igual al archivo `.sql` carácter por carácter, la regla de tabla completa contra encabezado más conteo, la matriz pivoteada de A1.3 con celdas vacías donde hay censura, la justificación de A1.6 entre 3 y 4 líneas, y la ausencia de cualquier hora de reloj en el documento. ~90 líneas. Requirement "reporte de resultados por ítem", ambos escenarios.
- [ ] 3.8 Modificar `tests/test_analysis_dataset_numbers.py`, marca `dataset`: agregar las 48 filas de A1.6, las medianas 13.5 contra 12.2 con tolerancia 0.05, y el MRR activo de A1.1 igual a la suma de `mrr_mxn` de las empresas activas en `master_dataset`. ~60 líneas. Requirement A1.1 (escenario "el total imputado coincide con la sábana"), A1.6 (escenario "48 tickets listados"). Decisión D12.
- [ ] 3.9 Modificar `tests/test_analysis_idempotency.py`, marca `dataset`: expandir la comparación a las ocho salidas completas de `analyze` (seis CSV, `analysis_exceptions.csv` y `report.md`), entre las dos corridas y contra la copia commiteada en `outputs/analysis/`, y mantener la verificación del hash de los ocho archivos de `outputs/` (A0, read-only) antes y después de correr `analyze`. Requirement: comando `analyze` sin build previo, escenario "dos corridas idénticas". Decisión D15.
- [ ] 3.10 Actualizar `README.md`: una sola línea nueva en "Ruta rápida" con el comando `python -m worky_engine analyze --data-dir fundation-docs --out-dir outputs/analysis` y una fila en la tabla de "Qué produce" para `outputs/analysis/report.md`. Sin em dashes.
- [ ] 3.11 Verificar que ningún archivo generado por `analyze` ni la prosa nueva de `report.py`, `README.md` o los comentarios SQL usa el carácter em dash (`—`); usar coma, dos puntos o punto y seguido en su lugar.
- [ ] 3.12 Cerrar PR 3: correr `python -m pytest -q -m "not dataset"`, correr `python -m pytest -q` completo con el dataset real (incluye la marca `dataset` de las tres pruebas de A1), correr `python -m worky_engine analyze --data-dir data/raw/sistemas --out-dir outputs/analysis` dos veces seguidas y confirmar que las ocho salidas quedan idénticas byte a byte, confirmar con `git status --short outputs` (A0, read-only) que los ocho goldens de A0 no cambiaron, generar y commitear `outputs/analysis/a1_06_negative_hours.csv`, `analysis_exceptions.csv` y `report.md`, y preparar el commit de cierre.

## Brechas encontradas en el diseño

- La tabla de líneas del PR 1 (sección 7 del diseño) lista `tests/test_analysis_idempotency.py` como creado por completo con "las ocho salidas" (D15), pero en el PR 1 solo existen tres de esas ocho salidas. Este documento crea el archivo en el PR 1 con las salidas disponibles y lo amplía en el PR 2 y el PR 3 (tareas 2.8 y 3.9), sin que la tabla de líneas del diseño liste esas dos modificaciones.
- La sección 6 del diseño exige una prueba de fixture para A1.6 en `tests/test_analysis_rules.py` ("en A1.6, el ticket negativo en el detalle y su fila de excepción"), pero la tabla de líneas del PR 3 no lista ninguna modificación a ese archivo. Este documento la agrega como tarea 3.6.
