# Tareas: script de limpieza automática de companies.csv (`a6-cleaning-script`)

Este documento convierte el diseño en trabajo ejecutable. El corte queda decidido de antemano y sigue las 26 escenarios de la especificación (`specs/data-cleaning/spec.md`, 11 requisitos). Los cuatro archivos de `outputs/clean/` quedan fuera del conteo de líneas de autoría, pero sí entran en la instantánea completa de revisión. Los archivos ya versionados bajo `outputs/analysis`, `outputs/health` y `outputs/warehouse`, además de `worky_engine/normalization/`, son de solo lectura para este cambio: ninguna tarea los edita, solo verifica su hash.

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | PR1 ~500 de autoría (diseño), PR2 ~400 de autoría (diseño). ~905 en total antes de aplicar el factor histórico de este repositorio (~2x, por ejemplo `cascade.py` de A0 midió 466 líneas contra 120 estimadas) |
| 400-line budget risk | Medium |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2, apiladas sobre `main` |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Presupuesto de autoría de este repositorio: 800 líneas por PR (`review_budget_lines`, `openspec/config.yaml`). El diseño (sección 9) ya estimó PR1 en ~500 y PR2 en ~400, cerca de ese tope, y advirtió que sus propias estimaciones se han quedado cortas por un factor de 2 en cambios previos. Por eso el riesgo queda en Medium y el corte en dos PR queda fijado desde ahora, sin esperar a medir.

Líneas de control exactas para el guardián automatizado:

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Medium
```

Con `auto-chain`, el orquestador procede con la primera rebanada usando `stacked-to-main`: PR1 apunta a `main`, PR2 apunta a la rama de PR1.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | Paquete `cleaning/`, lectura de CSV en texto, las tres detecciones (fechas, USD, mrr nulo con exclusión de clones), el log completo (json, md, exceptions csv), `cmd_clean` y el wrapper | PR1 | `python -m pytest -q -m "not dataset" tests/test_cleaning_rules.py` | `python -m worky_engine clean --data-dir fundation-docs --out-dir outputs/clean` y `python scripts/clean_companies.py --data-dir fundation-docs --out-dir outputs/clean` | Eliminar `worky_engine/cleaning/`, `worky_engine/quality/cleaning_contracts.py`, `scripts/clean_companies.py`, y revertir `cmd_clean` de `worky_engine/cli.py` |
| 2 | Imputación del ADR-002 con anualización, contratos de imputación y equivalencia, idempotencia contra los goldens ya versionados, y los cuatro goldens de `outputs/clean/` | PR2 | `python -m pytest -q -m "not dataset" tests/test_cleaning_imputation.py` | `python -m worky_engine clean --data-dir fundation-docs --out-dir outputs/clean` corrido dos veces seguidas | Eliminar `worky_engine/cleaning/impute.py`, revertir los contratos de imputación en `worky_engine/quality/cleaning_contracts.py`, borrar `outputs/clean/` |

## PR1: paquete `cleaning/`, las tres detecciones, el log y el comando `clean`

Rama: `feat/a6-pr1-clean-rules`, base `main`. Líneas de autoría estimadas: ~500. Qué revisa primero el revisor: que `mrr nulo` distinga las 28 filas clon de las 28 empresas reales sin imputarlas todavía, y que el comando y el wrapper generen exactamente la misma salida. Salvaguarda si este PR se acerca a 800 líneas: mover `cleaning_log.md` a PR2, porque el JSON ya cubre el requisito del caso y el Markdown es solo la versión legible (palanca de la sección 9 del diseño).

- [x] 1.1 Crear `worky_engine/cleaning/__init__.py` que exporta `run_clean` y `CleanResult`. D1.
- [x] 1.2 Crear `worky_engine/cleaning/rules.py` con las tres funciones puras: `normalize_dates` (usa `normalize_date` de `worky_engine/normalization/dates.py` (read-only), D4), `convert_currency` (usa `to_mxn` de `worky_engine/normalization/currency.py` (read-only), D5, D9) y `detect_missing_mrr` (separa las 28 filas clon `HS-9000xx` por regex de las 28 empresas reales imputables, D6 en su parte de detección). Lectura de CSV con `pd.read_csv(..., dtype=str, keep_default_na=False, encoding="utf-8")` (D3). Requisitos: "detección y normalización de signup_date", "conversión de moneda USD a MXN", "detección de mrr nulo con exclusión de clones", "detección de moneda USD".
- [x] 1.3 Crear `worky_engine/cleaning/runner.py` con `run_clean(companies, deals, ...) -> CleanResult` que orquesta fechas, moneda y detección de mrr nulo en ese orden fijo, y con `_passthrough_if_clean` que deja pasar de largo una fila cuyas cinco columnas de auditoría ya están completas (D8). La imputación se conecta en PR2. Requisito "idempotencia de la corrida" (parte de la lógica).
- [x] 1.4 Crear `worky_engine/quality/cleaning_contracts.py` con los contratos que no dependen de imputación: `assert_clean_row_count_preserved`, `assert_clean_columns_and_order`, `assert_currency_all_mxn`, `assert_dates_iso_or_empty`, `assert_exception_ids_unique`, `assert_counts_match_exceptions`, `assert_clean_is_idempotent`. Mismo `ContractViolation` de A0.
- [x] 1.5 Agregar el escritor del log a `worky_engine/cleaning/runner.py`: `cleaning_log.json` (esquema `worky.cleaning-log/v1`, sin marca de tiempo ni rutas absolutas, D10) escrito con `write_markdown`/`json.dumps(..., ensure_ascii=False, indent=2)`, `cleaning_log.md` con el resumen en español, y `cleaning_exceptions.csv` con las diez columnas de D11 (`exception_id`, `exception_code`, `source_system`, `source_id`, `field_name`, `original_value`, `applied_value`, `evidence_ref`, `confidence`, `ruleset_version`), ordenado por `exception_code`, `source_id` y `field_name`. En este PR solo emite los códigos `date_normalized`, `currency_converted_to_mxn` y `clone_excluded`. Requisito "contrato del log de limpieza".
- [x] 1.6 Modificar `worky_engine/cli.py`: agregar `_import_clean_dependencies`, `_resolve_clean_inputs`, `cmd_clean` (D2, D12) y el parser `clean` con `--data-dir` (por omisión `data/raw/sistemas`), `--out-dir`, `--companies`, `--deals`. Código 0 en éxito, 1 en contrato violado, 2 en archivo o dependencia faltante, con mensaje en español que nombra lo que falta. Requisitos "comando clean y wrapper equivalentes", "entradas requeridas companies.csv y deals.csv".
- [x] 1.7 Crear `scripts/clean_companies.py`, wrapper delgado que solo llama `main(["clean", *sys.argv[1:]])` (D12). Requisito "comando clean y wrapper equivalentes".
- [x] 1.8 Crear `tests/test_cleaning_rules.py` con fixture mínima propia (no extiende `tests/fixtures/mini_dataset.py`). Escenarios: "corrida exitosa desde ambas rutas", "falta companies.csv", "falta una dependencia", "ambos archivos presentes", "falta deals.csv", "un clon no se imputa", "fecha calendario imposible", "monto en USD", "monto ya en MXN", "mismas filas y mismo orden", "columnas originales más las nuevas". Prueba: `python -m pytest -q -m "not dataset" tests/test_cleaning_rules.py`.
- [x] 1.9 Crear `tests/test_cleaning_dataset_numbers.py`, marca `dataset`, versión parcial sobre `data/raw/sistemas/crm_hubspot__companies.csv` (read-only). Escenarios: "conteo exacto sobre el dataset" (56 con mrr nulo, 28 clones, 28 reales), "conteo exacto de USD" (22), "conteo y normalización de signup_date" (31, de las cuales 12 ambiguas), "churn_date sin mezcla de formatos" (0), "conteos exactos en json y en md" (versión parcial: 56, 28, 22, 31 y 12, sin deals anualizados todavía). Prueba: `python -m pytest -q -m dataset tests/test_cleaning_dataset_numbers.py`.
- [x] 1.10 Crear `tests/test_cleaning_idempotency.py`, versión parcial. Escenarios: "orden determinista", "cero correcciones sobre la salida propia", "salidas idénticas entre corridas" (sobre la fixture y sobre el dataset real sin imputación) y "ningún golden previo cambia" (confirma que `outputs/analysis`, `outputs/health` y `outputs/warehouse` no cambian). Prueba: `python -m pytest -q -m "not dataset" tests/test_cleaning_idempotency.py` y `python -m pytest -q -m dataset tests/test_cleaning_idempotency.py`.
- [x] 1.11 Cerrar PR1: correr `python -m pytest -q -m "not dataset" tests/test_cleaning_rules.py tests/test_cleaning_idempotency.py`, correr `python -m pytest -q -m dataset tests/test_cleaning_dataset_numbers.py tests/test_cleaning_idempotency.py`, correr `python -m pytest -q` (suite completa tal como existe en este punto), confirmar con `git status --short outputs` que no aparece ningún archivo (PR1 no escribe goldens de `outputs/clean/`), confirmar por sha256 que los veinte archivos ya versionados de `outputs/` (read-only) no cambiaron, y preparar el commit convencional (sin atribución de IA, sin coautor).

## PR2: imputación del ADR-002, `cleaning_exceptions.csv` completo y los goldens

Rama: `feat/a6-pr2-clean-impute`, base `feat/a6-pr1-clean-rules`. Líneas de autoría estimadas: ~400. Qué revisa primero el revisor: que las 28 imputaciones coincidan una a una con `outputs/exceptions_log.csv` (read-only) y `outputs/master_dataset.csv` (read-only), y que una tercera corrida sobre `companies_clean.csv` propio reporte cero correcciones. Salvaguarda si este PR se acerca a 800 líneas: recortar `test_cleaning_imputation.py` a los cuatro casos de la tabla del ADR-002 (alta, media, sin resolución, anualización) y mover los casos de borde de deals huérfanos a una prueba de seguimiento fuera de este corte.

- [ ] 2.1 Crear `worky_engine/cleaning/impute.py` con `impute_mrr_from_deals`: réplica en pandas de `worky_engine/sql/marts/mart_mrr.sql` (read-only) y `worky_engine/sql/marts/mart_deal_normalized.sql` (read-only) sobre `data/raw/sistemas/crm_hubspot__deals.csv` (read-only), unida por `hubspot_id` crudo. Incluye la normalización 12x (monto que es exactamente 12 veces otro de la misma empresa se trata como anual), `mrr_confidence` en `high` con un deal `closedwon` y `medium` en cualquier otro caso, y la elección del deal de evidencia por `(amount_mxn != candidate, deal_id)`. D6, D7 (tabla línea por línea del diseño, sección 3).
- [ ] 2.2 Modificar `worky_engine/cleaning/runner.py`: conectar `impute_mrr_from_deals` después de `detect_missing_mrr`, extender la regla `missing_mrr` del log con `corrected`, `excluded_clones` y `annualized_deals`, y agregar a `cleaning_exceptions.csv` las filas `mrr_imputed_from_deal` (con `deal_id` de origen en `evidence_ref` y `confidence` en `high` o `medium`) y `mrr_deal_annualized`. Requisitos "imputación de mrr desde deals con anualización" y "contrato del log de limpieza" (escenario "fila de excepción por corrección").
- [ ] 2.3 Modificar `worky_engine/quality/cleaning_contracts.py`: agregar `assert_no_null_mrr_after_imputation` y `assert_clone_deals_absent` (verifica que ningún deal apunte a un `hubspot_id` `HS-9000xx`, el supuesto que sostiene la equivalencia con `mart_mrr`).
- [ ] 2.4 Crear `tests/test_cleaning_imputation.py` con fixture mínima propia. Escenarios: "confianza alta", "confianza media", "empresa sin resolución", "anualización contada como corrección propia", "fila de excepción por corrección". Prueba: `python -m pytest -q -m "not dataset" tests/test_cleaning_imputation.py`.
- [ ] 2.5 Extender `tests/test_cleaning_dataset_numbers.py`: completar el escenario "conteos exactos en json y en md" agregando los 3 deals anualizados y las 112 filas totales de `cleaning_exceptions.csv`. Prueba: `python -m pytest -q -m dataset tests/test_cleaning_dataset_numbers.py`.
- [ ] 2.6 Crear `tests/test_cleaning_equivalence.py`, marca `dataset`. Escenario: "coincidencia con mart_mrr", comparando las 28 imputaciones contra `outputs/exceptions_log.csv` (read-only, filtrado a `mrr_imputed_from_deal`) por `source_id`, `applied_value` y `evidence_ref`, y el reparto 4 `high` / 24 `medium` contra `outputs/master_dataset.csv` (read-only). D7. Prueba: `python -m pytest -q -m dataset tests/test_cleaning_equivalence.py`.
- [ ] 2.7 Extender `tests/test_cleaning_idempotency.py`: completar "salidas idénticas entre corridas" comparando los cuatro archivos de `outputs/clean/` byte a byte entre dos corridas con imputación activa, y "ningún golden previo cambia" verificando por sha256 que los veinte archivos ya versionados de `outputs/` (read-only) siguen intactos con la imputación corriendo. Prueba: `python -m pytest -q -m dataset tests/test_cleaning_idempotency.py`.
- [ ] 2.8 Generar y commitear los cuatro archivos de `outputs/clean/` (`companies_clean.csv`, `cleaning_exceptions.csv`, `cleaning_log.json`, `cleaning_log.md`) como golden, fuera del conteo de autoría.
- [ ] 2.9 Modificar `README.md`: agregar el paso 7 de la ruta rápida (`python -m worky_engine clean` y el wrapper equivalente) y las filas de `outputs/clean/` en la tabla de salidas. A6 es dueño de esta sección mientras A5 corre en su propio worktree.
- [ ] 2.10 Cerrar PR2: correr `python -m pytest -q -m "not dataset" tests/test_cleaning_rules.py tests/test_cleaning_imputation.py tests/test_cleaning_idempotency.py`, correr `python -m pytest -q -m dataset tests/test_cleaning_dataset_numbers.py tests/test_cleaning_equivalence.py tests/test_cleaning_idempotency.py`, correr `python -m pytest -q` (suite completa con el dataset real), confirmar con `git status --short outputs` que solo aparece `outputs/clean/` y ningún archivo de `outputs/analysis`, `outputs/health` u `outputs/warehouse`, confirmar por sha256 que los veinte archivos ya versionados de `outputs/` (read-only) no cambiaron, y preparar el commit convencional (sin atribución de IA, sin coautor).
